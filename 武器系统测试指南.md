# 武器系统测试指南

## 一、编译和配置检查

### 1. 编译状态
✅ 所有代码已编译成功（Cinemachine 错误不影响功能）

### 2. 配置文件检查
- ✅ WeaponConfig.xlsx 已创建（4种武器）
- ✅ Item.xlsx 已添加武器（ID: 50009-50012）
- ✅ Equipment.xlsx 已添加武器装备配置
- ✅ Proto 消息已导出

## 二、Unity 编辑器配置（需要你手动完成）

### 1. 武器配置数据验证
在 Unity 中打开项目后：
1. 检查 `WeaponConfigCategory` 是否加载成功
2. 在 Inspector 中查看 4 种武器的配置：
   - 50009: 霰弹枪（散射4发，30度）
   - 50010: 步枪1号（追踪锁定）
   - 50011: 步枪2号（方向锁定）
   - 50012: 火箭炮（位置锁定）

### 2. HUD 武器槽位 UI 配置
需要在 HUDPanel 预制体中添加：

```
HUDPanel
├── WeaponSlot1Button (Button)
│   └── Text: "武器1"
├── WeaponSlot2Button (Button)
│   └── Text: "武器2"
├── AmmoText (Text)
│   └── 显示弹药: "弹药: 10/30"
└── ReloadingText (Text)
    └── 显示换弹状态: "换弹中..."
```

### 3. 按钮事件绑定
在 HUDPanelComponentSystem.cs 中添加按钮点击事件：

```csharp
// 在 YIUIInitialize 中绑定
self.u_EventWeaponSlot1Button.Add(() =>
{
    WeaponSwitchHelper.SwitchWeapon(self.Scene(), 1);
});

self.u_EventWeaponSlot2Button.Add(() =>
{
    WeaponSwitchHelper.SwitchWeapon(self.Scene(), 2);
});
```

### 4. 武器模型配置（可选）
如果需要显示武器模型：
1. 在角色 Unit 预制体上添加武器挂点（WeaponSocket）
2. 准备 4 种武器的模型预制体
3. 在 M2C_SwitchWeaponHandler 中加载对应模型

## 三、测试步骤

### 测试1：基础武器初始化
1. 启动服务器：`dotnet Bin/ET.App.dll --Console=1`
2. 启动 Unity 客户端
3. 登录并进入游戏
4. **预期结果**：
   - 角色成功进入地图
   - WeaponComponent 已初始化（查看日志）
   - 武器槽位显示正确的武器 ID

### 测试2：武器切换
1. 点击"武器1"按钮
2. 点击"武器2"按钮
3. **预期结果**：
   - 服务端日志显示切换消息
   - 客户端收到 M2C_SwitchWeapon 消息
   - 当前武器槽位更新

### 测试3：武器射击（需要配置 BT）
1. 在 Unity 中为 Unit 配置行为树
2. 添加以下节点序列：
   ```
   Sequence
   ├── BTWeaponNotMoving (检查静止)
   ├── BTWeaponHasTarget (检查目标)
   ├── BTWeaponInRange (检查范围)
   ├── BTWeaponCanFire (检查可射击)
   └── BTWeaponFire (执行射击)
   ```
3. 进入游戏，靠近敌人并静止
4. **预期结果**：
   - 武器自动射击
   - 子弹创建并飞向目标
   - 弹药消耗
   - 霰弹枪散射4发子弹

### 测试4：不同锁定类型
1. 切换到步枪1号（追踪锁定）
   - **预期**：子弹持续追踪移动的目标
2. 切换到步枪2号（方向锁定）
   - **预期**：子弹沿发射方向直线飞行
3. 切换到火箭炮（位置锁定）
   - **预期**：子弹飞向目标发射时的位置

### 测试5：英雄技能（桃子治疗）
1. 选择 HeroConfigId=1001 的英雄
2. 进入游戏后靠近友方单位
3. **预期结果**：
   - 每秒触发一次治疗
   - 5米范围内友方单位 HP 增加
   - 日志显示治疗信息

## 四、调试技巧

### 1. 查看日志
服务端日志位置：`Logs/`
关键日志搜索：
- `WeaponInitHelper`: 武器初始化
- `HeroSkill`: 技能触发
- `BulletHelper`: 子弹创建
- `WeaponSwitchHelper`: 武器切换

### 2. 常见问题排查

**问题1：武器没有初始化**
- 检查 LoadoutComponent 是否确认（IsConfirmed=true）
- 检查 EquipmentComponent 中是否有武器
- 查看 WeaponInitHelper 日志

**问题2：武器切换无反应**
- 检查 C2M_SwitchWeapon 消息是否发送
- 检查 WeaponComponent.CurrentSlot 是否更新
- 查看服务端 Handler 日志

**问题3：子弹不飞行**
- 检查 BulletComponent.Update 是否被调用
- 检查 FireLockType 是否正确
- 查看子弹的 FlyDirection 是否有效

**问题4：技能不触发**
- 检查 HeroSkillTimer 是否启动
- 检查 HealIntervalMs 是否 > 0
- 查看 AOI 范围内是否有友方单位

## 五、性能优化建议

1. **子弹池化**：使用对象池管理子弹 Unit
2. **AOI 优化**：技能扫描使用 AOI 而不是全局遍历
3. **消息合并**：弹药状态不需要每帧广播，可以按需发送
4. **BT 优化**：添加冷却时间避免频繁检查

## 六、下一步扩展

1. **武器配置表扩展**：
   - 添加子弹速度、最大距离配置
   - 添加武器音效、特效配置
   - 添加武器模型路径配置

2. **更多武器类型**：
   - 狙击枪（高伤害、慢射速）
   - 机枪（低伤害、高射速）
   - 榴弹发射器（AOE 伤害）

3. **技能系统扩展**：
   - 创建 SkillConfig 配置表
   - 支持多种技能类型（伤害、增益、控制）
   - 技能冷却和能量消耗

4. **UI 完善**：
   - 武器图标显示
   - 弹药进度条
   - 换弹动画
   - 准星和瞄准线

## 七、快速测试命令

```bash
# 1. 导出配置
cd /d/05ET/MatchTest/ETGame
dotnet ./Bin/ET.ExcelExporter.dll Luban

# 2. 导出 Proto
dotnet Bin/ET.Proto2CS.dll

# 3. 编译项目
dotnet build ET.sln

# 4. 启动服务器
dotnet Bin/ET.App.dll --Console=1

# 5. 查看日志
tail -f Logs/Log.txt
```

---

**所有代码已完成，现在需要你在 Unity 中配置 UI 并测试！**
