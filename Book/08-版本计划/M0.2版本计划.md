# M0.2 版本计划

**目标**：出领导体验包，验证核心体验（肉鸽胡牌 + 基础战斗）
**周期**：2026-03-23（周一）~ 2026-04-12（周日），共 3 周
**创建时间**：2026-03-26
**最后更新**：2026-04-16（已将 W1/W2 历史未完成任务统一并入 W3，后续以 W3 为唯一跟踪口径；W3 任务19“起装武器配置与 Weapon 全量同步”已完成本轮开发并推进到未回归：已把 `Item.xlsx` 与三份运行时 `ItemConfigCategory.json` 同步到 `50001~50011`，补齐服务端武器校验、商店购买门禁、客户端商店过滤、YIUI atlas 索引与一致性测试；`dotnet build ET.sln`、`Equipment_LoadoutShopBuyToFixedSlot_Test`、`Test_Loadout_WeaponConfigConsistency_Test` 已通过；但全量 Luban `Config` 导表仍被现有 `HomeBuilding.xlsx` 的 `BuildingType=主城` 解析错误阻塞，所以本轮额外手工回写了运行时 JSON 以保证武器同步即时生效；W3 任务18“Home收取异常与音频缺失临时止血”继续顺着 Hierarchy 里的 `LayerBlock` 收敛后确认，真实主因不只是旧 `LobbyPanel` 残留，而是 `LobbyPanelComponent.OnEventEnterMapInvoke` 作为 `UITaskEventP0` 点击事件默认会打开全局 `LayerBlock`，同时它又 `await SendMatchRequest()` 一直等到匹配成功和切图完成，导致输入屏蔽层被一路带进局内；本轮已在保留 `SceneChangeStart/Finish` UI 清理兜底的基础上，把匹配点击改为“确认起装 + 发匹配请求 + 打开等待窗”后立即返回，`Wait_MatchSuccess / Wait_SceneChangeFinish / CloseLobbyPanel` 改到后台协程继续执行，并补本地 `IsMatchFlowRunning` 防重入；`dotnet build ET.sln` 已通过；W3 任务11“射击打击感与武器差异化”本轮继续修正命中反馈缺口：现场确认 `HitFeedbackHelper` 旧实现依赖的 `_HitFlashAmount/_HitFlashColor` 在现有怪物材质中无人接入，因此怪物受伤不闪白；同时 `M2C_WeaponHitHandler` 在 `HitEffect` 为空时会提前 return，连带把闪白和缩放也跳过；本轮已改为通用材质闪白方案，直接驱动 `_BaseColor/_Color/_EmissionColor`，并拆掉该提前返回；`dotnet build ET.sln` 已通过；W3 任务17“家园系统文档体系重构与正式版demo规划”已按用户最新规则改写为建筑驱动：当前正式版 demo 围绕 `主城`、`大红收藏馆`、`物品回收间`、`农场`、`仓库` 组织，并明确 `主城任务组`、`建筑槽位`、`70% 拆除返还`、`建筑有内容禁止拆除` 等核心规则；同时把 `仓库` 收敛为当前优先细化模块，首版先做家园存储闭环，不接复杂背包整理；W3 任务2“经验条事件驱动改造”已完成客户端事件驱动收口：`MainPanel` 去掉肉鸽等级条轮询，玩家自身 `HPView` 改为事件驱动刷新，并保留非玩家单位显示等级轮询；`dotnet build ET.sln`、`Test_Rogue_ChooseOption_ClientFlow_Test`、`Test_Rogue_LevelUp_ChoiceFlow_Test` 已通过，状态推进到未回归；任务16“怪物技能/Buff/AI 静态配置化”已完成静态 BT 资产生成、导出和正式链路收口，并通过 `dotnet build ET.sln` 与 `Test_Rogue_Fixes_P0_Test`，状态推进到未回归；任务15“Home主界面需求评估与首期实现”已完成首期实现并推进到未回归，并已把 `Home` 首页静态骨架迁回 `LobbyPanel.BuildPanel` prefab；任务14已完成正式配置、导表与整仓编译验证；任务12已解除 `YIUIMCP` 当前编译阻塞、验证 `.port` 自动写回，并已实际驱动 `Home` 页面资源构建；任务29“Highlighters URP17 兼容修复”已补计划与设计入口，当前进入开发中，目标是收口第三方描边插件在 `com.unity.render-pipelines.universal 17.0.4` 下的编译阻塞；新增任务37“局内对局倒计时与超时撤离失败”，首版沿用 `RunTimeLimitComponent` 的 8 分钟权威主时限和现有 `1044 时间延长` 加成，客户端补常驻主倒计时 UI，并在到点后继续复用现有失败结算链路；新增任务38“任务点地图POI显示与追踪”，把局内任务点接入现有地图 POI 体系，进图后直接显示在小地图/大地图，支持单独任务点图标与文案，并复用现有唯一追踪逻辑）

## 版本目标

出一份可演示的领导体验包，重点验证两个核心体验：

1. **肉鸽胡牌体验**：打怪获得经验 → 升级 → 触发三选一选牌 → 凑成一套牌产生质变
2. **基础战斗体验**：摇杆移动（流畅无拉回）、自动攻击、武器切换

附加工作内容：休闲竞技赛道分析（Word 文档，不属于本项目代码）

## 当前基线

### 肉鸽系统（已有闭环）
- ✅ 杀怪加经验、升级、发牌、选牌挂 Buff — 服务端闭环已完成
- ✅ 客户端选牌面板、经验条展示 — UI 已有
- ✅ 配置体系完整（等级、经验、卡牌、升级数值）
- ✅ **41 张肉鸽牌已入当前配置**（当前仍按 7 大流派组织，但已不是旧文档中的“每派 5 张，共 35 张”口径）
- ✅ 发牌等级已配置：**1、3、5、8 级触发**（注意：旧文档写的 2/4/6/9 已过时）
- ✅ BTConfig 格式 `buff:xxxx` 为既定设计，解析为 BuffConfigId 挂 Buff，不走行为树
- ✅ Reroll 按钮已实现
- ✅ 客户端经验条已改为事件驱动（2026-04-11 完成 `MainPanel` 与玩家自身 `HPView` 收口）
- ⚠️ 无升级动画/音效/弹字反馈
- ⚠️ ShowTagsBuffId 当前只配了 1 档，2 张和 3 张流派效果会落同一个 Buff

### 战斗系统
- ✅ 摇杆移动基本可用
- ✅ 持续同向移动权威拉回抖动已修复并通过实机验收
- ✅ 自动攻击 — 完整可用（HasTarget→InRange→CanFire→Fire，目标选择权重 0.7/0.3，Buff 200200 驱动）
- ✅ 武器切换 — 服务端逻辑完整可用（双槽位，C2M_SwitchWeapon，弹药/换弹定时器完整）
- 🔴 武器切换 UI 有显示异常（BUG-001）
- ⚠️ 武器动画/模型挂接 — Animator 和 BindPoint 已有基础，完整状态机链路未实现（M0 可接受）
- ⬜ 长按丢弃武器（FEAT-001，M0.2 Backlog）

## 周计划索引

| 周 | 时间 | 周计划文件 | 状态 |
|----|------|-----------|------|
| W1 | 3/23（周一）~ 3/29（周日） | [M0.2-W1周计划.md](M0.2-W1周计划.md) | 进行中 |
| W2 | 3/30（周一）~ 4/5（周日） | [M0.2-W2周计划.md](M0.2-W2周计划.md) | 进行中 |
| W3 | 4/6（周一）~ 4/12（周日） | [M0.2-W3周计划.md](M0.2-W3周计划.md) | 进行中 |

## 版本级验收标准

### 肉鸽胡牌体验
- [ ] 打怪稳定获得经验，升级节奏合理
- [ ] 三选一选牌有明确的差异化选择感（7 大流派卡池已有，需验证生效效果可感知）
- [ ] 选牌后有可感知的质变（战斗体验明显不同）——依赖各牌效果实际实现，当前多数牌是配置占位
- [ ] 选牌 UI 有基本表现力（图标、描述清晰）

### 基础战斗体验
- [ ] 摇杆持续移动流畅，无明显拉回抖动
- [ ] 自动攻击正常工作
- [ ] 武器切换正常工作

### 体验包
- [ ] 休闲竞技赛道分析 Word 文档完成

## 变更记录

| 日期 | 变更内容 | 原因 |
|------|---------|------|
| 2026-03-26 | 初始创建（基于旧 M0.2 重写） | 版本目标变更为领导体验包，取消匹配系统任务 |
| 2026-03-26 | 对齐肉鸽基线：更新卡池数量（3→35）、发牌等级（2/4/6→1/3/5/8）、补充已知 ⚠️ 项 | 文档内容与工程实际不符 |
| 2026-03-26 | 对齐战斗基线：自动攻击/武器切换确认完整（✅），补充武器动画缺口 | 代码扫描核实 |
| 2026-03-26 | 武器切换 UI 异常录入 BUG-001；长按丢弃武器需求录入 FEAT-001 | 用户反馈 |
| 2026-03-27 | W2/W3 周计划填充完成 | 用户确认肉鸽效果修复为 W2 重点工作 |
| 2026-03-27 | 修正三周日期：W1=3/23~3/29, W2=3/30~4/5, W3=4/6~4/12 | 用户指出 W1 截止 3/29，共 3 周到 4/12 |
| 2026-03-27 | BUG-002 录入：起装进图后武器不攻击怪物 | 用户反馈，排 W1 P0 |
| 2026-03-27 | W2 新增任务9：小地图功能优化（角色居中+展开图标记+缩略图边缘显示） | 用户需求 |
| 2026-03-27 | W1 新增任务12：武器体验差异化设计 | 用户需求，P1 |
| 2026-03-27 | BUG-002 修复完成：起装进图后武器不攻击怪物 | 用户反馈已修复 |
| 2026-03-27 | 持续同向移动权威拉回抖动状态更新为已完成 | 用户实机验收通过 |
| 2026-03-27 | BUG-006 录入并完成代码修复：怪物死亡后尸体搜索阶段仍显示血条 | 用户反馈，排 W1 P0；客户端血条销毁清理逻辑已补齐，待实机场景回归 |
| 2026-03-27 | W1 新增任务15：ECA 文档与实现对齐清理 | 用户指出 ECA 文档与当前实现不一致，需清理旧文档并重写现行入口说明 |
| 2026-03-27 | ECA2 文档首轮清理完成：删除 3 份过时历史文档，重写 2 份现行入口文档 | 避免继续引用 `ECAConfigAsset`、手动接线、旧撤离流程和错误默认值 |
| 2026-03-27 | 怪物文档决策更新：5只都做、资源先占位、侦察猴去掉致盲、每只怪新建独立AI | 用户确认实现方向，补充概念文档与技能/AI设计文档，并登记到 W2/W3 计划 |
| 2026-03-27 | W1 任务7/8 启动开发，新增《肉鸽效果修复开发日志》 | 按 `et-plan` / `et-design` 门禁进入肉鸽效果逐牌核查与 P0 修复 |
| 2026-03-27 | P0 定向测试首个失败定位到 `1013`，并完成旧 `ItemId` 兼容修复首轮验证 | `dotnet build ET.sln`、`Test_Rogue_Fixes_P0_Test`、`Ecanode_KeyDoor_Toggle_Test` 已通过 |
| 2026-03-27 | 怪物技能与AI一期/二期代码完成并进入未回归 | 5 只怪首版技能与独立 AI 已落地，`dotnet build ET.sln` 通过，待场景内手工回归 |
| 2026-03-28 | W1 任务7逐牌核查完成，确认当前实际卡池为 41 张并产出《肉鸽牌效果逐牌核查表》 | 旧文档中的 35 张口径已过时，后续任务8按核查表中的真实偏差项推进 |
| 2026-03-28 | W1 任务7旧文档同步完成，状态推进到已验收 | 已把《肉鸽功能实现梳理》《肉鸽效果修复计划》等旧文档回写到 41 张卡牌新基线 |
| 2026-03-28 | W1 新增任务16：场景组件 ECA 化设计，并新增《场景组件ECA化设计文档》 | 用户要求先形成实施文档，暂不开始代码实现 |
| 2026-03-28 | W1 任务11 验收退回：死亡结算时 `Settlement` 弹出后瞬间消失并回到大厅 | 用户人工验收不通过，任务状态从“未回归”退回“开发中” |
| 2026-03-29 | W1 任务11 二次修复完成并推进到未回归 | 已定位根因为 `SceneChangeFinish_ShowSettlementPanel` 在 `LobbyPanel` 打开前提前打开结算页，现已改为 `Home + LobbyPanel` 就绪后再打开；`dotnet build ET.sln` 通过 |
| 2026-03-29 | W1 任务11 三次时序调整完成，目标改为“结算先展示、关闭后回主界面” | 已改为收到结算消息立即打开 `SettlementPanel`、后台切场回 `Home`、`Home` 到达时压住 `LobbyPanel`，结算关闭后再打开大厅；`dotnet build ET.sln` 通过 |
| 2026-03-29 | W1 任务11 用户确认验收通过，状态推进到已验收 | 用户确认死亡路径当前表现符合“结算先展示、后台换场景、关闭后回主界面”的预期 |
| 2026-03-29 | W1 任务7 用户确认完成，状态保持已验收 | 任务7已完成，后续仅继续推进任务8中的真实偏差修复 |
| 2026-03-29 | W1 任务14 BUG-006 已验收完成 | 用户确认“验收通过，任务完成”；任务状态推进到已验收，BUG追踪更新为已修复 |
| 2026-03-29 | W1 任务4 文档收口完成，状态推进到未人工验收 | 已补齐怪物/机器人 AI、服务发现演练、任务/家园前后端闭环说明，当前等待人工确认文档口径 |
| 2026-03-29 | W1 新增任务17：BUG-008 搜索界面一键拾取后物品未进背包 | 用户反馈，排 W1 P0；按共用链路修复并补 `BUG-008` 修复方案/开发日志 |
| 2026-03-29 | W1 任务17 BUG-008 完成代码修复并推进到未回归 | 已下沉二维合法找位到 `cn.etetet.item`，同步修复一键拾取/整理背包/卸装备回包；新增 `Item_AddMultiGridPlacement_Test` 与 `dotnet build ET.sln` 均已通过 |
| 2026-03-30 | W1 任务17 BUG-008 回归失败，任务退回开发中 | 用户实机确认“部分物品失败”仍存在；进一步定位到 `ContainerMoveItem` 的容器拖拽入包链路仍未走二维合法找位，且旧配置 `10012` 会被按 `1x1` 直接写入背包，需继续修复并补回归 |
| 2026-03-30 | W1 任务17 BUG-008 第二轮修复完成并重新推进到未回归 | 已补 `ContainerMoveItem` 的容器拖拽入包/回包服务端二维兜底，`ItemHelper.MoveItem` 也新增二维合法锚点校验；旧配置 `10012` 入包会归一到 `22012` 并写入真实尺寸；`dotnet build ET.sln` 与 3 条相关自动化回归全部通过 |
| 2026-03-30 | W1 任务17 BUG-008 第三轮补空背包客户端提示，状态维持未回归 | 继续复盘现场日志确认本局 live unit 没有背包，失败表现符合“空背包可进图”规则；用户明确选择不改规则，因此在 `SearchPanel` 补一键拾取与拖拽到背包区的空背包提示，`dotnet build ET.sln` 再次通过 |
| 2026-03-30 | 肉鸽效果修复自动化回归完成，人工验收安排到 W3 | `dotnet build ET.sln`、`Test_Rogue_(Fixes_P0|Fixes_P1|HealSpirit_ImmediateTick)_Test`、`Test_Rogue.*` 已通过；用户确认在 2026-04-06 ~ 2026-04-12 进行局内人工验收 |
| 2026-03-30 | W3 任务7 进入设计中，新增《Windows客户端离线打包设计文档》 | 用户确认体验包打包需自动切 `CodeMode=Client`、`OfflinePlayMode`，并自动构建 YooAsset AB；当前先完成文档评审，不额外生成 zip |
| 2026-03-30 | W3 任务7 推进到开发中，新增《Windows客户端离线打包开发日志》 | 用户确认开始开发；本轮实现将覆盖 Unity 批处理入口、YooAsset AB 构建、Windows 客户端构建和 PowerShell 一键脚本 |
| 2026-03-30 | W2 新增任务11：BUG-009 匹配机器人无法攻击怪物 | 用户反馈机器人可见怪物但不会攻击；现场日志已确认自动开火链路存在目标获取/控距/射程判定不一致，按 BUG 修复流程立项 |
| 2026-03-30 | W2 任务11 BUG-009 完成代码修复并推进到未回归 | 已统一修复自动开火目标复用、开火距离判定和机器人控距逻辑，并更新现有 AI 说明文档；`dotnet build ET.sln` 通过 |
| 2026-03-31 | W2 新增任务12：FEAT-002 局内GM按UnitConfigId召唤怪物 | 用户要求在局内 GM 面板增加按钮+输入框，按 `UnitConfig.Id` 在玩家前方固定距离召唤怪物，便于验证怪物 AI 和技能 |
| 2026-03-31 | W2 任务12 FEAT-002 完成代码实现并推进到未回归 | 已补客户端 GM 命令、地图服召唤 Handler/Helper、Proto 与错误码，`dotnet build ET.sln` 通过，待局内验证怪物 AI 和技能 |
| 2026-03-31 | 明确 BUG 与需求分轨：W1/W2 周计划中的 BUG 条目迁移到 `Book/BUG追踪.md`，周计划仅保留需求/Feature，并保留历史引用编号 | 用户要求 bug 与需求分开管理 |
| 2026-03-31 | 补齐 `BUG-009/010/011/012` 到 `Book/BUG追踪.md`，并将原周计划 `BUG-003 HeroList` 更正为 `BUG-011` | 整理周计划遗留 BUG，消除编号冲突 |
| 2026-03-31 | W2 原任务13拆分为 `BUG-010 索敌遮挡收敛` + `FEAT-003 移除手动锁定功能` | 用户要求 bug 与需求分开 |
| 2026-03-31 | 新增 BUG-012：匹配成功后匹配界面未隐藏 | 用户反馈地图传送回 `Home` 后匹配界面残留，需要在匹配成功后立即隐藏 |
| 2026-03-31 | BUG-012 完成代码修复并待回归 | 已在匹配成功后关闭等待弹层，并在 `LobbyPanel` 打开时补兜底清理；`dotnet build ET.sln` 通过 |
| 2026-03-31 | W2 新增任务13：BUG-010 索敌遮挡收敛并移除手动锁定功能 | 用户确认玩家/机器人/怪物索敌统一收敛；接受基于 NavMesh/Recast 的遮挡判定；并要求删除手动锁定功能；本轮先产出设计文档，不开始编码 |
| 2026-04-01 | W2 新增任务14：武器优化专项 | 用户要求将当前武器 UI / 交互 / 战斗连续性缺口统一按需求整理成专项，不按 bug 处理；本轮先产出专项设计文档并拆分 Feature Backlog |
| 2026-04-01 | W2 新增任务15：FEAT-008 局内安全格拾取与保留 | 用户确认本期正式支持局内安全格运行时：搜索界面实时显示安全格、支持高价值优先拾入安全格、容器/背包/安全格拖拽，以及死亡保留与结算回写 |
| 2026-04-01 | W2 任务15 FEAT-008 完成代码、Luban 导表与定向回归，推进到未人工验收 | 已通过 `dotnet .\Bin\ET.ExcelExporter.dll Luban`、`dotnet build ET.sln`、`dotnet .\Bin\ET.App.dll --SceneName=Test --TestName='(PlayerCorpseLoot.*Insurance|ContainerMoveToBagPlacement)' --Console=1`，当前等待局内手工验收搜索界面显示/拖拽体验 |
| 2026-04-01 | W2 任务13 FEAT-003 进入开发中并新增《索敌遮挡与移除手动锁定开发日志》 | 用户确认可以开始开发；按 `et-plan` / `et-design` 门禁先同步计划与开发文档 |
| 2026-04-01 | W2 任务13 FEAT-003 代码完成并进入未回归 | 已完成 LoS 下沉、手动锁定删除、点击敌人不锁敌和客户端语义收口；`dotnet build ET.sln`、`Test_TargetSelector_Basic_Test`、`Test_Navmesh_SDCMap_LineOfSight_Test`、`Test_ClickUnit_Enemy_NoLock_Test` 均通过 |
| 2026-04-01 | W2 任务13 FEAT-003 追加收口“场景直点交互”链路 | 用户明确点击 ECA / NPC / 任务单位也必须删除；本轮已停用客户端场景点击、单位信息面板点击与 `C2M_ClickUnitRequest` 业务语义，仅保留按钮交互链路，`dotnet build ET.sln` 与 3 条定向测试再次通过 |
| 2026-04-01 | 需求追踪从 `Book/BUG追踪.md` 拆分到 `Book/需求追踪.md`，并修正武器优化专项文档的 `Reload` 协议与换弹进度设计 | 文档审查发现 Feature 与 BUG 仍混表，且武器专项文档中 `ILocationMessage + ResponseType` 组合不自洽，需统一修正 |
| 2026-04-09 | W3 任务4/8 用户确认完成，状态推进到已验收；新增 W3 任务11「优化射击打击感」P0 | 场景概念设计与休闲竞技分析完成；射击打击感为体验包核心体验，涉及音效/动作/特效/数值，动作需区分一次性与持续开火 |
| 2026-04-09 | W3 新增任务12：接入 YIUI MCP 并进行体验 | 用户提出新任务；当前先按工具链/界面开发前置事项录入计划，后续进入开发时联动 `et-design` 与 `yiui-unity-mcp` |
| 2026-04-09 | W3 任务11 合并 W1 任务12「武器体验差异化设计」，产出《射击打击感与武器差异化设计文档》，状态推进到设计中 | 用户确认合并；设计文档涵盖动作差异化（单发/连发）、枪口火焰、相机抖动、命中反馈、音效确认、5 把枪数值全面重调 |
| 2026-04-10 | W3 新增任务13：怪物AI说明文档补充 | 用户要求在对应位置创建怪物 AI 文档；已补《怪物AI行为说明.md》，记录普通怪物 `300001` 与运行时 MonsterProfile 怪物的真实入口、参数和行为边界 |
| 2026-04-10 | W3 任务12 推进到设计中，新增《YIUI MCP接入与报错排查说明》 | 用户要求查看导入报错并整理工具说明；当前已确认首个阻塞为 `YIUIMCPLog.cs` 依赖 `Unity.EditorCoroutines.Editor` 但包未声明 `com.unity.editorcoroutines`，同时记录 `UTO/node_modules` 导入过重与 `.port` 未生成导致脚本超时这两个接入问题 |
| 2026-04-10 | W3 任务12 推进到开发中，新增《YIUI MCP编译阻塞修复设计文档》与《YIUI MCP编译阻塞修复开发日志》 | 用户明确允许开始，并要求先修当前真正挡住编译的依赖错误；本轮按最小修复原则只处理 `com.unity.editorcoroutines` 依赖缺失 |
| 2026-04-10 | W3 新增任务14：怪物正式配置化 | 用户要求把影刃猫/侦察猴/幻影猫头鹰/重装鳄鱼/巴鲁正式配进怪物表，后续地图 ECA 直接填正式 `UnitConfigId`；本轮同步创建《怪物正式配置化设计文档》《怪物正式配置化开发日志》，并按“正式 UnitConfig + 独立 MonsterCorpseLootConfig + 复用尸体箱 1002 + 废弃旧字符串入口”推进 |
| 2026-04-10 | W3 任务12 修复 `YIUIMCPLog.cs` 当前编译阻塞 | 最终未保留“补包依赖”方案，而是改为在 `YIUIMCPLog` 内部移除 `EditorCoroutineUtility` 编译期依赖；Unity 最新错误已前移到 `cn.etetet.statesync` 的现有问题 |
| 2026-04-10 | W3 任务12 继续修复 `YIUIMCPToolBar.cs` 当前编译阻塞，并跑通基础调用链 | 已去掉对缺失 YIUI 编辑器 API 的强依赖，改为反射兼容和菜单兜底；`/health` 与 `Config/get_console_log.ps1` 已跑通 |
| 2026-04-10 | W3 任务12 完成 `.port` 自动写回验证 | 实测删除 `Packages/cn.etetet.yiuimcp/UTO/.port` 后，再通过 `TriggerCompile` 触发 Unity 编译/域重载，文件会在 11:00:01 自动重建为 `3212`；说明动态端口同步链路已恢复 |
| 2026-04-10 | W3 任务12 梳理完整人工体验流程，并修复 `invoke-uto-tool.ps1` 单工具入口不稳定问题 | 已新增《YIUI MCP完整人工体验流程》；同时把 `invoke-uto-tool.ps1` 改为持有临时 UTO 进程句柄，当前 `get_console_log.ps1`、`invoke-uto-tool.ps1`、`compile-unity-flow.ps1` 三个 CLI 入口均已实测通过 |
| 2026-04-10 | W3 任务14 完成正式配置化并推进到未回归 | 已新增 5 个正式 `UnitConfig(1012~1016)`、5 条独立 `MonsterCorpseLootConfig`，旧字符串入口改为废弃兼容态；`dotnet .\Bin\ET.ExcelExporter.dll Luban` 与 `dotnet build ET.sln` 已通过，待后续摆点与场景回归 |
| 2026-04-10 | W3 新增任务15：Home主界面需求评估与首期实现 | 用户要求必须做 `Home` 方向真实未完成功能；当前已新增《Home主界面需求文档》《Home主界面开发日志》，并确定首期目标为“进入 Home 默认落 Home 主界面 + 基地总览 + 建造/升级/拆除/收取首段闭环”，实现载体先复用 `LobbyPanel.BuildPanel` |
| 2026-04-10 | W3 任务15 完成首期实现并推进到未回归 | 已打通 `Home` 入图快照、客户端运行时缓存、`建造/升级/拆除/收取` 请求，以及 `LobbyPanel.BuildPanel` 内的 `Home` 首页承接；`dotnet build ET.sln` 已通过，待执行完整人工流程回归 |
| 2026-04-10 | W3 任务12 / 任务15 追加 `Home` 资源层验证 | 已新增 `ET/YIUI/Build Home Resources` 编辑器菜单，并通过 `YIUIMCP ExecuteMenu` 成功驱动 `Home` 首页静态骨架写入 `LobbyPanel.BuildPanel` prefab；运行时代码已收掉整页建壳，`dotnet build ET.sln` 再次通过 |
| 2026-04-10 | W3 新增任务16：怪物技能/Buff/AI 静态配置化 | 用户要求不要长期保留“正式 UnitConfig + 运行时 MonsterRuntimeProfile”两套怪物链路；本轮新增静态配置化任务，目标是把 5 只怪的 Spell/Buff/AI 收口到老静态 BT 资产与导出配置链路 |
| 2026-04-10 | W3 任务16 完成静态 BT 资产落地并推进到未回归 | 已生成 25 个怪物 `Spell/Buff/AI` 静态 BT 资产，导出 `SpellConfigCategory.txt / BuffConfigCategory.txt`，并收口正式刷怪/GM/测试入口；`dotnet build ET.sln` 与 `Test_Rogue_Fixes_P0_Test` 已通过，待场景内人工回归 |
| 2026-04-10 | W3 新增任务17：家园系统文档体系重构与正式版demo规划 | 用户要求将家园文档重构为《家园系统需求文档》《家园UI设计文档》《家园系统开发日志》三层结构，并保留旧 `Home主界面需求文档` 作为历史稿；后续正式版 demo 以新文档为准 |
| 2026-04-11 | W3 任务2 完成经验条事件驱动改造并推进到未回归 | 已新增 `EventRogueProgressChanged`，`MainPanel` 与玩家自身 `HPView` 改为事件驱动刷新；`dotnet build ET.sln`、`Test_Rogue_ChooseOption_ClientFlow_Test`、`Test_Rogue_LevelUp_ChoiceFlow_Test` 已通过，待 Unity 局内手工回归 |
| 2026-04-13 | W3 任务17 文档口径修正为建筑驱动，并明确 `仓库` 为当前优先细化模块 | 用户连续确认家园正式版必须围绕场景建筑组织；本轮已把新文档从抽象模块切到 `主城 + 建筑槽位 + 4 个可建建筑` 结构，并把仓库首版范围收敛为家园存储闭环 |
| 2026-04-13 | W3 任务17 追加《家园数据驱动加载与仓库正式页设计文档》，确认当前真正阻塞为 Home 正式配置表缺失 | 继续往实现层收口时确认：若不先补配置，只能硬编码主城/仓库 ConfigId、槽位和容量，违反项目根规范；因此当前正确切口是先补 Home 最小配置表 |
| 2026-04-13 | W3 任务17 继续落地主城任务、收藏馆和仓库正式承接，并推进到未回归 | 已新增 `HomeMainCityTask` 配置表并完成导表；服务端接通主城任务即时计算、主城升级前置、收藏馆摆放/取下和按 `ItemUid` 精确取物；客户端首页新增 `主城任务` / `收藏馆管理` 子模式，点击仓库建筑时可直接跳现有正式仓库页；`dotnet build ET.sln` 验证后只剩既有旧测试错误 `Test_PlayerCorpseLoot_Insurance_Test.cs(423,73)` |
| 2026-04-13 | W3 新增任务18：Home收取异常与音频缺失临时止血 | 用户现场日志显示 `HomeCollect` 业务错误被客户端异常化，同时多个音频 key location 无效；本轮先做最小止血恢复可玩性 |
| 2026-04-13 | W3 任务18 完成代码止血但仍维持开发中 | `HomeCollect` 异常化与音频缺 key 问题已完成最小止血；但两次 `dotnet build ET.sln` 都被既有测试文件 `Test_PlayerCorpseLoot_Insurance_Test.cs(423,73)` 的类型不匹配错误阻塞，暂不能推进到 `未回归` |
| 2026-04-13 | W3 任务18 补充修正真正主因：`LobbyPanel` 残留挡住肉鸽牌点击 | 最新 `Editor.log` 显示 `OnEventEnterMapInvoke` 在匹配成功后关闭 `MatchView` 时空引用，中断了 `LobbyPanel` 收尾关闭；已改为 `root.YIUIMgr()` 兜底关闭等待窗与大厅面板 |
| 2026-04-13 | W3 新增任务19：起装武器配置与 Weapon 全量同步 | 用户反馈“应该配了 9 个武器，起装只能看到 5 个”；核查确认 `Weapon.xlsx` 已扩到 `50001~50011`，但 `Item.xlsx` 起装武器仍停留在旧 `50009~50013` 口径，本轮按 `Weapon.xlsx` 当前全量收口 |
| 2026-04-13 | W3 任务19 完成代码与运行时同步并推进到未回归 | 已同步 `Item.xlsx` 与三份运行时 `ItemConfigCategory.json` 到 `50001~50011`，补齐服务端武器校验、商店购买门禁、客户端商店过滤、YIUI atlas 索引与一致性测试；`dotnet build ET.sln`、`Equipment_LoadoutShopBuyToFixedSlot_Test`、`Test_Loadout_WeaponConfigConsistency_Test` 已通过；全量 Luban `Config` 导表仍被现有 `HomeBuilding.xlsx` 解析错误阻塞，因此本轮采用手工回写运行时 JSON 保证即时生效 |
| 2026-04-13 | W1/W2 历史未完成任务统一并入 W3 | 已将未在 W3 显式登记的历史遗留补录到 W3#20~#26，并把 W1#1/W2#1、W1#8/W2#5、W1#12/W2#14 等任务显式挂接到现有 W3 任务；后续以 W3 作为唯一跟踪口径 |
| 2026-04-13 | W3 新增任务27：局内任务点刷怪奖励闭环 | 用户要求用 ECA 配置完成“靠近任务点提示交互、点击后接受任务、刷一波怪、清怪给固定金币、同局只可完成一次”的完整闭环；任务点场景摆放由用户自行负责 |
| 2026-04-13 | W3 任务27 完成源码实现并补齐文档，动态回归暂被外部旧错误阻塞 | 已完成服务端任务点闭环、客户端接受任务弹窗、FlowGraph 模板与定向测试源码；但 `dotnet build ET.sln` 仍被既有 `Test_PlayerCorpseLoot_Insurance_Test.cs(423,73)` 阻塞，当前 `Bin/ET.Hotfix.dll` 未刷新到新测试类，需待外部阻塞解除后继续回归 |
| 2026-04-16 | W3 任务27 补任务点参数模板自动下发 | 用户确认任务点参数应直接在 Inspector 里可见，不应再手敲 `mission_task_*` key；本轮已让含 `StartTask` 的 FlowGraph 自动把模板同步到 `ECAPointMarker.Params`，并把 `RogueMissionTaskFlow` 默认值写进模板 |
| 2026-04-13 | W3 新增任务28：匹配卡住问题排查与修复 | 用户反馈“点击匹配后卡住”仍未解决；复核确认旧 `BUG-012` UI 修复和 `HomeStorage` Gate handler 修复都已落地，当前需单独收口匹配成功后的服务端转图问题 |
| 2026-04-13 | W3 任务28 完成代码修复并推进到未回归 | 已新增 `GateMapTransferHelper` 统一临时建 Unit + 正式 Transfer，登录进 `Home` 与匹配兜底共用同一条链路；匹配成功时 location 转图失败会自动 fallback 到 GateMap 转图，同时 `G2Map_LogoutHandler` 现在会清理 `LocationType.Unit`；`dotnet build ET.sln` 已通过 |
| 2026-04-13 | W3 任务18 继续修正肉鸽牌点击问题，改为场景切换生命周期统一清理 root UI | 最新日志与截图确认仅修匹配协程尾部关闭还不稳；本轮把 `LobbyPanel/HomePanel` 清理下沉到 `SceneChangeStart`，并在进入非 `Home` 地图后由 `SceneChangeFinish` 再做一次兜底，避免旧大厅层残留挡住肉鸽牌 |
| 2026-04-13 | W3 任务18 编译验证通过并推进到未回归 | `dotnet build ET.sln` 已通过；当前等待你进游戏复验进图后局内 Hierarchy 是否已清干净，肉鸽牌点击是否恢复 |
| 2026-04-13 | W3 任务17 追加修正 HomePanel 打开卡死，改为只刷当前页并收口重复强刷 | 用户反馈点击基地按钮后界面卡死；静态排查确认根因是 `HomePanel` 首次打开时把 `总览/任务/收藏馆/回收间/农场/仓库` 全部重建，并在农场/回收态按秒触发整页刷新；本轮已新增定时节流、只渲染当前可见页，并避免进入 `Home`/首次打开时重复强刷，`dotnet build ET.sln` 已通过 |
| 2026-04-14 | W3 任务18 继续收敛 `LayerBlock` 根因并修正匹配点击长 await | 已确认 `LayerBlock` 来自 YIUI 全局输入屏蔽层，而 `LobbyPanel` 的 `OnEventEnterMapInvoke` 作为 `UITaskEventP0` 点击事件把整条匹配/切图链路都 await 住，导致输入屏蔽被带进局内；本轮已把匹配点击改为短返回、后台协程继续等待进图，并补本地 `IsMatchFlowRunning` 防重入；`dotnet build ET.sln` 已再次通过 |
| 2026-04-14 | W3 任务11 修正“怪物受伤不闪白” | 现场确认旧命中反馈依赖的 `_HitFlashAmount/_HitFlashColor` 在现有怪物材质中空打；同时 `HitEffect` 为空会把闪白和缩放一起提前跳过；本轮已改为通用材质闪白方案，直接驱动 `_BaseColor/_Color/_EmissionColor`，并拆掉提前返回；`dotnet build ET.sln` 已通过 |
| 2026-04-14 | W3 任务11 二次收口“通用材质闪白仍不明显” | 进一步确认许多怪物材质的 `_BaseColor/_Color` 乘子本来就是纯白，lerp 到白色几乎无可见增量；本轮已把闪白改成“亮度乘算 + 原始 emission 叠加”，并收敛到网格 renderer；最新 `dotnet build ET.sln` 重试暂被 `ET.Model.dll` 文件锁阻塞，待进程释放后补验证 |
| 2026-04-14 | W3 任务11 二次调强后的整仓编译补验证通过 | 文件锁释放后再次执行 `dotnet build ET.sln`，当前“亮度乘算 + 原始 emission 叠加”版本已整仓编译通过（0 warnings / 0 errors） |
| 2026-04-14 | W3 任务11 继续修正“持枪 idle / 走路表现同态” | 进一步排查确认玩家实际使用 `HeroBase.controller`，问题不是资源没接上，而是连发武器 `IsFiring` 只有置 true 和换弹清零，正常停火没有回落点；本轮已在客户端武器视图里补“最后一发时间 + 本地超时 + 切枪/换弹强制清零”收口；`ET.Hotfix` / `ET.HotfixView` 已成功编出，完整 `dotnet build ET.sln` 后续被并行进程占用 `ET.Aspire.dll` 阻塞 |
| 2026-04-16 | W3 新增任务29：Highlighters URP17 兼容修复 | 用户要求修复第三方描边插件在 `URP 17.0.4` 下因旧 `RenderTargetHandle / cameraColorTarget / Blit` 兼容写法导致的编译阻塞 |
| 2026-04-16 | W3 任务29 完成第三方 Highlighters URP17 兼容修复并推进到未回归 | 已将 `Assets/Highlighters-Outlines/URP` 中的 `RenderTargetHandle / cameraColorTarget / GetTemporaryRT / 旧 Blit` 迁移到 `RTHandle / cameraColorTargetHandle / ReAllocateHandleIfNeeded / Blitter`，并补齐跨 pass 目标引用与资源释放；`dotnet build ET.sln` 已通过 |
| 2026-04-16 | W3 新增任务30：ECA ConfigId 补齐工具支持重复ID自动修复 | 用户要求 `ET/ECA/Fill Missing ConfigIds` 不仅补空 `ConfigId`，还要在检测到重复 `ConfigId` 时自动修复 |
| 2026-04-16 | W3 任务30 完成代码实现并推进到未回归 | 已增强 `ApplyECAConfigIdEditor.FillMissingConfigIds()`：空 `ConfigId` 继续补齐，重复 `ConfigId` 则保留首项并自动重分配后续重复项；同时补充工具统计输出；`dotnet build ET.sln` 已通过 |
| 2026-04-16 | W3 任务20 从“迷雾参数调整”升级为“视野遮挡收敛”并重新进入开发中 | 用户回归确认当前迷雾、单位本体与小地图标记仍存在穿墙可见；本轮要求与索敌统一复用 Recast LoS，保留团队共享视野，且 `Ward/Detector` 也受墙体遮挡 |
| 2026-04-16 | W3 新增任务31：起装背包与安全箱自适应布局 | 用户要求 `LobbyPanel` 里的背包背景、安全箱背景与格子区域贴合，并让安全箱自动排在背包下方；当前先按文档驱动流程补计划与设计入口，再收口运行时布局 |
| 2026-04-16 | W3 任务14 追加客户端 prefab 名对齐 | 用户摆入新怪 prefab 后，现场发现 `1012~1016` 透明；排查确认客户端当前直接用 `UnitConfig.Name` 拼 `Bundles/Units/{Name}.prefab`，本轮已把 `Unit.xlsx` 与三份运行时 `UnitConfigCategory.json` 的 `Name` 对齐为 `XiaoMao/XiaoHou/XiaoYing/XiaoE/BearBoss` |
| 2026-04-16 | W3 任务31 完成代码并推进到未回归 | 已在 `LobbyPanel` 运行时缓存背包/安全箱相对布局，按当前格子尺寸重排 `Bg/BoardRoot/Title/Hint`，并让安全箱自动跟随背包块高度下移；`dotnet build ET.sln` 已通过，待 Unity 手工回归视觉与交互 |
| 2026-04-16 | W3 新增任务32：地图 POI 显示与追踪 | 用户要求首期在大小地图显示“我方撤离点 + 高级容器”，仅大地图可点击弹 tips 并设为唯一追踪目标，小地图需显示追踪目标边缘指引；文本走通用 `Text.xlsx`，后续 `Boss刷新点` 复用同一套机制 |
| 2026-04-16 | W3 新增任务33：局内 SearchPanel 背包与安全箱自适应布局 | 用户要求把局外 `LobbyPanel` 已完成的背包/安全箱自适应布局能力同步到局内 `SearchPanel`，并覆盖 `BackpackInspect / ContainerSearch / CorpseLoot` 三模式；本轮先补计划与设计入口，再按“prefab 容器 + 运行时尺寸刷新”收口 |
| 2026-04-16 | W3 任务33 完成代码并推进到未回归 | 实际落地改为“运行时 `BagRoot` + 块级缓存刷新 + 打开时恢复布局基线”，避免直接手改 `SearchPanel.prefab` 大段 YAML，同时保留现有 YIUI 绑定与拖拽/点击语义；`dotnet build ET.sln` 已通过 |
| 2026-04-16 | W3 任务33 补双栏模式安全箱区域基线修复 | 用户在尸体搜索回归里发现安全箱和容器区重叠；已把 `ContainerSearch / CorpseLoot` 调整为保留 `SecureBagRoot` 原始区域，仅 `BackpackInspect` 继续纵向堆叠；`dotnet build ET.sln` 已通过 |
| 2026-04-16 | W3 新增任务34：搜索/起装界面物品双击快速转移 | 用户要求在搜索物品界面与起装界面补双击整堆快速转移：SearchPanel 支持容器<->背包，Loadout 支持固定槽双击卸回仓库、仓库双击放入背包、背包/安全箱双击回仓库；失败静默，不额外提示 |
| 2026-04-16 | W3 新增任务35：局内地面物品拾取提示 Panel（PickupHintPanel） | 用户要求正式落地 `PickupHintPanel`；当前已确认代码层基本就绪，但 prefab 尚未创建、旧指南资源路径过时，且 `MainPanel` 仍只走 SearchButton 临时拾取链路；本轮按 YIUI 正式资源构建 + 主入口联动收口 |
| 2026-04-16 | W3 新增任务36：XiaoMao普通攻击冲刺与特效配置 | 用户要求把影刃猫唯一普通攻击收口为“前摇预警 -> 冲刺 -> 冲刺结束附近一次范围伤害”，前摇默认 `1.5s` 可配，前摇播 `XiaoMaoYuJing`，冲刺时自身播 `XiaoMaoGongJi`，不做终点扇形收尾 |
| 2026-04-16 | W3 新增任务37：局内对局倒计时与超时撤离失败 | 用户要求在局内增加一个常驻对局总倒计时 UI，并在倒计时结束后判定撤离失败；首版确认沿用当前 `8 分钟` 权威主时限与 `1044 时间延长` 加成，失败结果继续复用现有失败结算链路 |
| 2026-04-16 | W3 任务37 完成 prefab 主倒计时切换并通过编译/定向测试 | 已把 `MainPanel` 主倒计时从运行时创建切到 `MainPanel.prefab` 静态节点，补齐对应 YIUI 绑定与 skill 规则；`dotnet build ET.sln`、`Test_RunTimeLimit_ClientSync_Test` 已通过，当前等待局内人工验收 |
| 2026-04-16 | W3 新增任务38：任务点地图POI显示与追踪 | 用户要求把局内任务点也接入现有地图 POI，进图后直接显示在小地图/大地图，并使用单独任务点图标与文案 |
| 2026-04-16 | W3 任务38 完成代码实现并推进到未回归 | 已新增 `MapPoiType.MissionTask`、任务点参数识别、任务点专用图标/文案/颜色配置，并同步 `Text.xlsx / MinimapConstConfig.xlsx` 与运行时 JSON；`dotnet build ET.sln` 已通过 |
| 2026-04-16 | W3 任务37 完成主倒计时同步与 UI 主链，整仓编译暂被既有协议缺失阻塞 | 已补 `M2C_RunTimeLimitState`、客户端 `RunTimeLimitClientComponent`、`MainPanel` 顶部运行时文本、服务端进图/加时/清理同步与定向测试源码；但 `dotnet build ET.sln` 当前先被仓库里既有 `WeaponDiscarded / PickupGroundItem / DiscardWeapon` 协议类型缺失拦住，任务状态暂维持“开发中” |
| 2026-04-16 | W3 新增任务39：局内武器丢弃与地面拾取协议补齐及编译修复 | 用户要求修复 `M2C_WeaponDiscarded / C2M_DiscardWeapon / C2M_PickupGroundItem` 等类型缺失编译错误；按已有丢枪/拾取设计补齐协议与 opcode，并恢复 `dotnet build ET.sln` |
| 2026-04-16 | W3 任务19 / 32 / 38 追加正式图标配置 | 用户提供 11 个正式武器图标与 `MapIcon` POI 图标；本轮已把武器 icon 切到 `weapon_50001~weapon_50011`，地图 POI 切到 `poi_*` 命名，并同步 `Item.xlsx`、`MinimapConstConfig.xlsx`、运行时 JSON、`YIUIAtlasData.asset` 与 `SDCMap.txt` |
| 2026-04-16 | W3 新增任务40：局内主动退出按钮与成功撤离 | 用户要求在局内 `MainPanel` 常驻新增退出按钮；点击后不做二次确认，直接按现有撤离成功链路展示完整结算页并退出到大厅 |
| 2026-04-16 | W3 新增任务40：局内主界面图标与拾取提示字体修复 | 用户反馈 `MainPanel` Buff 区和武器栏图标显示成白块，同时地面武器拾取提示 `PickupHintPanel` 错用英文字体；本轮按 prefab 真实层级与现有中文 TMP 字体资产统一收口 |
| 2026-04-16 | W3 任务40 完成代码实现并推进到未回归 | 已把 Buff 区图标绑定改为命中 `ActionBarSlot/Icon`，给 `WeaponItem.prefab` 补独立 `WeaponIcon` 并拆分背景/图标缓存，同时把 `PickupHintPanel` 切到中文字体 `江城圆体 600W_爱给网_aigei_com SDF`；`dotnet build ET.sln` 已通过 |
| 2026-04-16 | W3 新增任务41：怪物描边分级显示 | 用户要求普通怪加红色描边、精英/Boss 加橙色描边；核查发现仓库内已无 `Highlighters-Outlines` 插件源码与程序集，当前改为客户端运行时 URP 轮廓壳层方案 |
| 2026-04-16 | W3 任务27 调大任务点刷怪随机范围并推进到未回归 | 用户明确要求“只改参数，改成 `10 -10`”；本轮沿用现有硬编码刷怪散布逻辑，把任务点 `offsetX/offsetZ` 从 `[-2,2]` 调整到 `[-10,10]`，并重新执行 `dotnet build ET.sln` 通过 |
