# 局内主动退出按钮与成功撤离开发日志

**功能**：局内主动退出按钮与成功撤离
**关联设计文档**：[局内主动退出按钮与成功撤离设计文档.md](./局内主动退出按钮与成功撤离设计文档.md)
**关联任务**：M0.2-W3 #40
**开始时间**：2026-04-16
**开发者**：AI

## 开发进度

- [x] 步骤1：补计划与设计/开发文档
- [x] 步骤2：新增主动退出请求/响应与错误码
- [x] 步骤3：补服务端主动成功撤离 handler
- [x] 步骤4：修改 `MainPanel.prefab` 并接入退出按钮点击
- [ ] 步骤5：补测试并执行编译验证

## 决策记录

### 2026-04-16 - 主动退出不复用撤离点倒计时链前半段
- **背景**：现有成功撤离入口绑定在撤离点 ECA 交互上，要求玩家站点内停留一段时间。
- **方案**：只复用 `EvacuationSettlementHelper` 和成功结算页，不复用撤离点范围检测与倒计时。
- **原因**：用户要求的是“局内任意时刻点击按钮立即成功撤离”，语义和撤离点局部交互已经不同。
- **替代方案**：伪造一个隐藏撤离点并强行走 `PlayerEvacuationComponent`。放弃原因是会把 UI 按钮错误耦合到场景点位语义，链路更脆。

### 2026-04-16 - 退出按钮优先落 MainPanel prefab
- **背景**：用户明确要求后续所有涉及 UI 的修改优先考虑 prefab，不要运行时创建。
- **方案**：直接在 `MainPanel.prefab` 补静态退出按钮，再由手写层接入点击。
- **原因**：按钮属于局内主界面的稳定常驻区块，资源层表达更直观，也方便你直接进游戏验证。
- **替代方案**：运行时代码创建按钮。放弃原因是违反当前明确约束。

### 2026-04-16 - 主动退出改为后台协程执行结算与切图
- **背景**：`Test_QuickExitExtraction_SettlementFlow_Test` 日志显示主动退出后能收到 `M2C_QuickExitExtraction`、`M2C_StartSceneChange` 和 `M2C_HomeSnapshot`，但没有 `M2C_EvacuationSettlement`，导致成功结算页运行态无法稳定建立。
- **方案**：`C2M_QuickExitExtractionHandler` 先同步返回“请求接受成功”，再在后台协程里执行 `EvacuationSettlementHelper.Settle(unit)` 与 `TransferHelper.TransferAtFrameFinish(...)`。
- **原因**：普通撤离成功链本身不包在 location RPC 响应里；当前异常只出在“同一条 location RPC 内同时做结算和切图”的路径，先把执行形态向现有成功链对齐，优先恢复结算消息下发。
- **替代方案**：继续维持同步执行，仅调整测试等待条件。放弃原因是这样只能绕过测试，不足以保证真正的成功结算页链路成立。

## 问题日志

### 2026-04-16 - 仓库存在并行脏改动
- **现象**：当前工作树里已有倒计时、协议、UI、家园和第三方插件等多条并行改动。
- **原因**：仓库不是干净状态。
- **解决**：本轮只修改任务 40 相关文件，不回退、不覆盖其他任务线。

### 2026-04-16 - 整仓编译被移动同步线阻塞
- **现象**：执行 `dotnet build ET.sln` 时，`cn.etetet.statesync` 的移动同步相关文件出现缺失/不一致：`C2M_MoveStateHandler.cs`、`M2C_MoveSyncHandler.cs`、`MoveValidationComponentSystem.cs` 触发 `CS2001`，并伴随 `MoveValidationComponentSystem.cs` 上的 `ET0037`。
- **原因**：这是当前工作区里与任务 40 无关的并行改动，直接阻塞整仓编译和后续自动测试。
- **解决**：本轮不擅自修复无关任务线；先记录阻塞，待工作区恢复可编译后继续验证主动退出链。

## 变更清单

| 时间 | 文件 | 操作 | 说明 |
|------|------|------|------|
| 2026-04-16 | `Book/08-版本计划/M0.2-W3周计划.md` | 修改 | 新增 W3 任务 40 |
| 2026-04-16 | `Book/08-版本计划/M0.2版本计划.md` | 修改 | 追加版本变更记录 |
| 2026-04-16 | `Book/05-肉鸽玩法/局内主动退出按钮与成功撤离设计文档.md` | 新增 | 创建设计文档 |
| 2026-04-16 | `Book/05-肉鸽玩法/局内主动退出按钮与成功撤离开发日志.md` | 新增 | 创建开发日志 |
| 2026-04-16 | `Packages/cn.etetet.statesync/Proto/StateSync_C_10700.proto` | 修改 | 新增主动退出请求/响应 |
| 2026-04-16 | `Packages/cn.etetet.proto/CodeMode/Model/Client/StateSync_C_10700.cs` | 修改 | 同步客户端协议生成代码 |
| 2026-04-16 | `Packages/cn.etetet.proto/CodeMode/Model/ClientServer/StateSync_C_10700.cs` | 修改 | 同步 ClientServer 协议生成代码 |
| 2026-04-16 | `Packages/cn.etetet.proto/CodeMode/Model/Server/StateSync_C_10700.cs` | 修改 | 同步服务端协议生成代码 |
| 2026-04-16 | `Packages/cn.etetet.statesync/Scripts/Model/Share/ErrorCode.cs` | 修改 | 新增主动退出错误码 |
| 2026-04-16 | `Packages/cn.etetet.statesync/Scripts\Hotfix\Server\C2M_QuickExitExtractionHandler.cs` | 新增 | 服务端主动退出入口，现已改为后台协程执行结算与切图 |
| 2026-04-16 | `Packages/cn.etetet.statesync/Assets/GameRes/YIUI/Main/Prefabs/MainPanel.prefab` | 修改 | 静态增加局内常驻退出按钮 |
| 2026-04-16 | `Packages/cn.etetet.statesync/Scripts/ModelView/Client/YIUIComponent/Main/MainPanelComponent.cs` | 修改 | 增加退出按钮运行时引用 |
| 2026-04-16 | `Packages/cn.etetet.statesync/Scripts/HotfixView/Client/YIUISystem/Main/MainPanelComponentSystem.cs` | 修改 | 绑定退出按钮点击并发送主动退出请求 |
| 2026-04-16 | `Packages/cn.etetet.test/Scripts/Hotfix/Test/Test_QuickExitExtraction_SettlementFlow_Test.cs` | 新增 | 增加主动退出成功链定向测试 |

## 开发总结

- **实际完成**：已补计划和文档门禁；已补主动退出协议/错误码、服务端 handler、`MainPanel.prefab` 常驻退出按钮、客户端按钮点击链和定向测试；同时针对主动退出链里 `M2C_EvacuationSettlement` 未稳定下发的问题，把服务端“结算+切图”从同步 location RPC 内挪到后台协程。
- **未完成**：整仓编译与定向测试还没有在当前工作区完成闭环验证。
- **与设计的偏差**：服务端主动退出响应语义从“执行完成后返回 success”调整为“请求已接受并开始处理后返回 success”，结算页和回大厅仍走原权威链。
- **后续待办**：待工作区恢复可编译后，重新执行 `dotnet build ET.sln` 与 `Test_QuickExitExtraction_SettlementFlow_Test`，确认成功结算页完整链路恢复。
