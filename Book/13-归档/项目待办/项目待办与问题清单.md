# 项目待办与问题清单

## 1. 审查范围

- 审查时间：2026-03-18
- 审查方式：静态代码审查 + `dotnet build ET.sln`
- 编译结果：通过，`0 warning / 0 error`
- 测试结果：未执行完成，测试场景启动被当前环境策略拦截，因此本清单以代码现状为准

## 2. 总结

当前项目里，`装备起装 / 搜包 / 武器栏` 这一类功能链路相对完整；真正明显“还没做完”或“已经有后端但没接上层/UI”的，主要集中在：

- 任务系统
- 家园系统
- 成就系统
- ActionBar 到施法输入的最后一段绑定

其中优先级最高的是任务和家园，因为这两块存在明显的主链路断点，做 UI 之前先补最小闭环更合适。

## 3. 问题与待办表

| 优先级 | 模块 | 类型 | 现状 | 关键证据 | 建议动作 |
|---|---|---|---|---|---|
| P0 | 任务 | 功能未闭环 | 登录后没有真正同步任务数据；服务端接任务后发 `M2C_CreateQuest`，客户端没有对应处理；现有客户端更新逻辑还会对不存在的任务直接写状态。 | `Packages/cn.etetet.quest/Scripts/Hotfix/Client/ClientQuestEventSystem.cs:45`；`Packages/cn.etetet.quest/Scripts/Hotfix/Server/QuestHelper.cs:12`；`Packages/cn.etetet.quest/Scripts/Hotfix/Client/QuestComponentSystem.cs:16` | 先补 `M2C_CreateQuest` 客户端 Handler；登录后直接调用现成 `QuestHelper.SyncQuestData`；`UpdateQuestData` 改成“不存在则创建”。 |
| P0 | 家园 | 功能未接客户端/UI | 家园 Proto 已有，但客户端和服务端 Handler 实际没落地；进入 `Home` 仍然打开的是 `LobbyPanel`，没有真正的 Home 界面。 | `Packages/cn.etetet.home/Proto/Home_C_15600.proto:55`；`Packages/cn.etetet.home/Doc/home-ui-plan.md:742`；`Packages/cn.etetet.map/Scripts/HotfixView/Client/Scene/SceneChangeFinishEvent_CreateUIHelp.cs:16` | 先做最小闭环：`Snapshot + Build + Collect` 的请求/响应 Handler，再做最基础的 Home 面板。 |
| P0 | 家园 | 运行时问题 | 进入 Home 时只挂了 `PlayerHomeComponent`，但生产和合同逻辑都依赖 `HomeProductionComponent` / `HomeContractComponent`。 | `Packages/cn.etetet.home/Scripts/Hotfix/Server/Helper/HomeEnterHelper.cs:21`；`Packages/cn.etetet.home/Scripts/Hotfix/Server/Helper/HomeProductionHelper.cs:30`；`Packages/cn.etetet.home/Scripts/Hotfix/Server/Helper/HomeContractHelper.cs:25` | 进入 Home 或加载家园数据时，把这两个组件一并初始化。 |
| P0 | 成就 | 功能未接入 | 成就代码骨架在，但没有找到玩家 Unit 挂载 `AchievementComponent` 的地方，也没有看到 `Initialize()` 被调用。 | `Packages/cn.etetet.achievement/Scripts/Model/Server/AchievementComponent.cs:9`；`Packages/cn.etetet.achievement/Scripts/Hotfix/Server/AchievementComponentSystem.cs:24` | 先把 `AchievementComponent` 挂到玩家 Unit，再补初始化入口。 |
| P1 | 成就 | 功能半成品 | 成就配置未接、激活逻辑未做、奖励是假数据、分类名和图标是默认占位、详情内容是占位字符串。 | `Packages/cn.etetet.achievement/Scripts/Hotfix/Server/AchievementSystem.cs:14`；`Packages/cn.etetet.achievement/Scripts/Hotfix/Server/AchievementComponentSystem.cs:161`；`Packages/cn.etetet.achievement/Scripts/Hotfix/Server/C2M_ClaimAchievement_Handler.cs:27`；`Packages/cn.etetet.achievement/Scripts/Hotfix/Server/C2M_GetAchievementCategories_Handler.cs:88`；`Packages/cn.etetet.achievement/Scripts/Hotfix/Server/C2M_GetAchievementDetail_Handler.cs:27` | 对接 AchievementConfig 和奖励系统，统一从配置生成详情、分类和奖励预览。 |
| P1 | 成就 | 缺客户端落地 | 客户端只有 `ClientAchievementHelper` 和数据实体，没有看到组件挂载、缓存同步、View、Panel、HotfixView。 | `Packages/cn.etetet.achievement/Scripts/Hotfix/Client/ClientAchievementHelper.cs:11`；`Packages/cn.etetet.achievement/Scripts/Model/Client/ClientAchievementComponent.cs:9` | 先做客户端缓存组件初始化，再做最小成就列表界面。 |
| P1 | 任务 | 有服务端没接 UI | 点击 NPC 时服务端已经会回 `questInfo`，客户端只留了“显示任务界面”的注释，没有任务对话框。 | `Packages/cn.etetet.map/Scripts/Hotfix/Server/Map/C2M_ClickUnitRequestHandler.cs:31`；`Packages/cn.etetet.map/Scripts/Hotfix/Client/Unit/UnitClickHelper.cs:15` | 做一个最小任务对话框，支持可接/进行中/可提交三态。 |
| P1 | 任务 | 目标同步不完整 | `M2C_UpdateQuestObjectiveHandler` 还是 TODO；同步任务时客户端没有恢复 `NeedCount`，可能导致目标完成判定失真。 | `Packages/cn.etetet.quest/Scripts/Hotfix/Client/M2C_UpdateQuestObjectiveHandler.cs:16`；`Packages/cn.etetet.quest/Scripts/Hotfix/Client/M2C_SyncQuestDataHandler.cs:31`；`Packages/cn.etetet.quest/Scripts/Hotfix/Client/QuestObjectiveSystem.cs:18` | 扩协议或补配置反查，把 `NeedCount`、目标说明一起恢复到客户端。 |
| P1 | 任务 | 接口缺口 | `QueryAvailableQuests` 服务端还是空壳；客户端写了 `AbandonQuest` 和 `GetQuestDetail`，但未看到对应服务端 Handler。 | `Packages/cn.etetet.quest/Scripts/Hotfix/Server/C2M_QueryAvailableQuestsHandler.cs:6`；`Packages/cn.etetet.quest/Scripts/Hotfix/Client/QuestHelper.cs:120`；`Packages/cn.etetet.quest/Scripts/Hotfix/Client/QuestHelper.cs:189` | 把 `QueryAvailableQuests / AbandonQuest / GetQuestDetail` 先补齐。 |
| P1 | 任务 | 文档与实现不一致 | `README` 里仍写着 `ClientQuestComponent / ClientQuestData / ClientQuestObjectiveData / ClientQuestHelper`，但当前实际类名和结构已经不是这一套。 | `Packages/cn.etetet.quest/README.md:23` | 补完主链路后顺手更新模块 README，避免误导。 |
| P1 | 家园 | 设计已写但代码没跟上 | `home-ui-plan.md` 里列出了客户端组件、Handler、View、事件，但这些文件大多还没创建。 | `Packages/cn.etetet.home/Doc/home-ui-plan.md:733`；`Packages/cn.etetet.home/Doc/home-ui-plan.md:742`；`Packages/cn.etetet.home/Doc/home-ui-plan.md:755`；`Packages/cn.etetet.home/Doc/home-ui-plan.md:775` | 直接按这份计划落第一版，不要再另开新设计。 |
| P1 | 家园 | Handler 目录空 | `Scripts/Hotfix/Server/Handler` 与 `Scripts/Hotfix/Client/Handler` 当前都没有实际处理器文件。 | `Packages/cn.etetet.home/Scripts/Hotfix/Server/Handler/`；`Packages/cn.etetet.home/Scripts/Hotfix/Client/Handler/` | 先补 `C2M_HomeBuildRequest / C2M_HomeCollectRequest / M2C_HomeSnapshot` 三类处理。 |
| P1 | 家园 | 硬编码问题 | 生产时长、产出物、合同数量、合同奖励、收集速率都写死在 Helper 里。 | `Packages/cn.etetet.home/Scripts/Hotfix/Server/Helper/HomeProductionHelper.cs:10`；`Packages/cn.etetet.home/Scripts/Hotfix/Server/Helper/HomeCollectHelper.cs:10`；`Packages/cn.etetet.home/Scripts/Hotfix/Server/Helper/HomeContractHelper.cs:10` | 尽快配置化，至少先从表里读时长、产出、奖励。 |
| P2 | 战斗/UI | 最后一段未接通 | ActionBar 到施法输入没有真正绑定，`spellConfigId` 被写死为 `0`。 | `Packages/cn.etetet.map/Scripts/HotfixView/Client/InputSystemComponentSystem.cs:258` | 从 ActionBar 实际槽位读取技能 ID，再触发施法。 |
| 已处理 | 战斗/UI | 旧占位实现残留 | 旧 `HUDWeaponSlotComponentSystem` 已删除，武器栏统一走 `WeaponBar` 新链路。 | `Packages/cn.etetet.statesync/Scripts/HotfixView/Client/YIUISystem/Main/WeaponBarComponentSystem.cs:16` | 当前无需继续处理，后续只维护 `WeaponBar / WeaponItem`。 |

## 4. 推荐实施顺序

### 第一批：先补闭环

1. 任务主链路
2. 家园最小闭环
3. 成就接入入口

### 第二批：再补可见层

1. 任务 NPC 对话框 / 任务面板
2. 家园主界面
3. 成就列表与详情界面

### 第三批：清理和配置化

1. 家园硬编码配置化
2. 成就配置化
3. 清理旧 UI 占位代码
4. 更新 README 和设计文档

## 5. 我建议你下一步先做什么

如果按收益排序，建议先做下面这个顺序：

1. 任务：补 `M2C_CreateQuest`、登录同步、目标同步
2. 家园：补 `HomeSnapshot + Build + Collect` 的请求/响应和客户端缓存
3. 成就：先把组件真正挂上并初始化，再接配置

这样做完以后，项目里“功能有代码但实际上玩不起来”的几块大缺口会先被堵上。
