# 持续同向移动拉回修复设计文档

**创建时间**：2026-03-26
**最后更新**：2026-03-29
**状态**：开发中
**关联任务**：M0.2-W1 #2
**涉及包**：cn.etetet.map, cn.etetet.statesync

## 需求概述

当前摇杆移动在以下场景仍存在明显手感问题：

1. 玩家朝同一方向持续推动摇杆时，角色会被服务端权威位置周期性拉回。
2. 日志显示服务端持续广播同向移动结果，但客户端每次收到权威包后都会把本地前向预测清零。
3. 用户目标不是“靠阈值掩盖抖动”，而是恢复平地持续移动时客户端预测与服务端结果基本一致的旧手感，同时保持现有“沿墙滑动但绝不穿墙”。
4. 最新日志表明，剩余抖动不只发生在停步或贴墙场景，持续移动阶段也会出现 `targetPos` 跳变与角色抖动。
5. 2026-03-27 晚间新的 Unity 编辑器日志进一步确认，停步等待 `stop ack` 的阶段还存在“可见层被硬钉死”的问题，体感表现为明显卡顿。

## 问题定位

最新日志和代码链路复核后，当前剩余问题已经收敛为两类客户端预测语义错误：

1. 在没有新权威包到达的间隙，客户端 `AdvancePrediction` 仍会持续累加 `PredictedDelta`。
2. 当累计前置量超过一个合理预测窗后，`targetPos` 会逐步远离 `authPos`。
3. 下一次权威包到达时，客户端 reconcile 会按新的权威位置重建回放结果，`targetPos` 被迫回撤或改向，于是体感上表现为持续移动中的抖动/瞬移。
4. 这段问题日志里大量样本同时满足 `blocked=False`、`hold=False`、`visualCorrection=(0,0,0)`，说明根因不是撞墙缓冲本身，而是客户端预测层内部的前置量管理失衡。
5. 另外，在玩家已经松开摇杆、客户端已经发送 `stop seq` 之后，服务端仍会继续确认前几个旧移动输入；当前 `PendingStopActive` 分支把 `replayPosition` 直接钉在旧 `viewPos`，导致 `ackInputSeq` 虽在推进，但 `targetPos/viewPos` 不跟随 `authPos` 收敛，于是体感上表现为“移动非常卡”。
6. 2026-03-29 最新日志进一步确认，平地持续移动时客户端会长期稳定保留约 `0.31m` 的 authority gap；该数值与本地 lead 窗口上限一致，说明问题已经从“预测丢失”收敛为“预测前置窗过大”。
7. 同一批日志还表明，异常诊断 warning 在 Unity Editor 下会持续刷屏并放大卡顿，因此诊断机制本身也需要改为默认静默。

## 目的

本轮修复目标明确如下：

1. 保留“沿墙滑动但绝不穿墙”的现有能力，不回退服务端墙约束。
2. 恢复平地持续同向移动时，客户端预测与服务端结果基本一致的顺滑手感。
3. 杜绝客户端预测在权威包间隙无上限累积，避免持续移动阶段的周期性抖动和回撤。
4. 保持停步、转向、贴墙、离墙恢复等既有场景不被这轮修复破坏。
5. 修正停步等待期语义：玩家松手后不再继续执行 stale tail 本地前推，但显示层也不能因为等待 `stop ack` 而被硬钉死在旧视图位置。

## 技术方案

### 整体思路

沿用“服务端权威 + 客户端本地 NavMesh 约束预测 + 输入回执回放”的现有骨架，但修正客户端预测前置量的生成和回放边界。

当前实现只会：

1. 以服务端权威位置为起点，回放尚未确认的输入历史。
2. 把回放结果作为新的 `PredictedDelta`。

前几轮修复已经解决了以下问题：

1. 当玩家持续按住同一方向时，当前输入虽然已经被服务端确认，但客户端渲染层仍然需要保留一个短前向 lead。
2. 现实现里一旦“未确认历史输入”为空，`PredictedDelta` 就会在每个权威包到达时被重置为 0。
3. 这会让客户端在“本地前进一小段”与“权威包清零预测”之间反复拉扯，形成持续被拉回的手感。

但最新日志说明，还存在第二层问题：

1. 即使保留了活跃输入 lead，如果 `PredictedDelta` 在权威包间隙继续无限增长，后续权威回放仍然会产生明显回撤和抖动。
2. 因此修复不能只处理“每包清零”，还必须限制“持续增长”和“回放重建”两个阶段的预测前置量。

修复原则：

1. `PendingMoveInputs` 只负责“未确认历史输入回放”。
2. 当前仍在按住的活跃输入，需要在权威回放之后继续保留前向预测 lead。
3. `PredictedDelta` 只表示逻辑回放结果，不再承担“显示层避免后退抖动”的职责。
4. 如果新的回放目标会退到当前 `viewPos` 后方，则把这部分回退量单独放入显示层缓冲，并按可视速度逐步回收。
5. 这个 lead 和缓冲后的目标仍然必须继续走同一套客户端本地 NavMesh 约束，不能绕开碰撞。
6. 停步、撞墙、方向突变、权威显著偏离等场景，仍然允许现有强纠偏逻辑生效。
7. 本轮新增“双层限幅”：
   - 运行中增长限幅：`AdvancePrediction` 对持续累加的 `PredictedDelta` 做上限控制。
   - 权威回放重建限幅：`TryReconcileAuthoritativeMove`、`ClampReplayPredictionPosition`、`ApplyAuthoritativeReplay` 共同限制回放阶段可重建的前置量。
8. `PendingStopActive` 在 stop ack 到达前，不再把回放目标钉死在当前视图；改为直接丢弃 stale tail 的本地重放结果，只保留“向权威位置平滑收敛”的显示层行为。
9. 2026-03-29 新增“前置窗收紧 + 诊断默认静默”：
   - 将持续移动/站立阶段的最大 prediction lead 从原先偏大的窗口收紧到更贴近实时输入节奏的短窗，避免平地持续移动时长期稳定领先 authority 约 `0.3m`。
   - `DiagAuthViewGap`、`DiagPendingStopConverge`、`DiagReconcileStoppedGap`、`DiagVisualDrift` 改为编译期开关控制，默认不进入运行时热路径，只在需要专项定位时显式开启。
10. 2026-03-29 夜间新增“停步语义回退验证”：
   - 因为你要求先和上一次提交的版本对齐，当前先只回退 `PendingStopActive` 这条链路到 `94a9c8a7e` 的停步冻结语义。
   - 这次回退仅用于验证“最近的手感变化是否就是由 stop 等待期改成 authority 收敛导致”，其余如日志静默、lead 窗口恢复到 `0.12f` 等改动保持不变。

### 涉及的包和文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `Packages/cn.etetet.map/Scripts/HotfixView/Client/InputSystemComponentSystem.cs` | 修改 | 修正 stop 等待期 stale tail 回放语义，并在自机权威回放后先限幅 `replayPosition` |
| `Packages/cn.etetet.map/Scripts/HotfixView/Client/Unit/GameObjectComponentSystem.cs` | 修改 | 调整 `AdvancePrediction` 与 `ApplyAuthoritativeReplay`，限制预测前置量持续增长与回放重建 |
| `Packages/cn.etetet.map/Scripts/HotfixView/Client/ChangePosition_SyncGameObjectPos.cs` | 视需要修改 | 保持自机 ChangePosition 链路与新回放语义一致 |
| `Packages/cn.etetet.statesync/Scripts/Hotfix/Client/M2C_JoystickMoveHandler.cs` | 视需要修改 | 关闭移动热路径默认 trace，避免编辑器日志量反向放大卡顿体感 |
| `Packages/cn.etetet.test/Scripts/Hotfix/Test/Test_ClientInputReplayReconciliation_Test.cs` | 修改 | 补充 target 回退/抖动回归用例，并增加持续移动前置窗上限回归 |
| `Book/02-战斗系统/移动系统/持续同向移动拉回修复开发日志.md` | 修改 | 实时记录开发步骤与验证结果 |

### Entity/Component 设计

本次不新增新的 Entity 类型，优先复用现有组件：

1. `InputSystemComponent`
2. `UnitViewInterpolationComponent`
3. `JoystickMoveAuthorityStateComponent`

如现有字段无法表达“活跃输入前向 lead”语义，再补充最小必要字段，避免职责继续混杂。

### 接口设计

网络协议不变，本次继续依赖：

1. `M2C_JoystickMove.LastProcessedInputSequence`
2. `C2M_JoystickInput.InputSequence`

修复重点在客户端融合逻辑，不调整协议字段。

## 实现步骤

1. 根据最新日志确认“持续同向输入时 `PredictedDelta` 被每包清零”和“持续移动阶段 `PredictedDelta` 继续失控增长”的触发条件。
2. 修改客户端 reconcile，使其在回放未确认输入后保留当前活跃输入的前向预测 lead，并继续经过本地 NavMesh 约束。
3. 在 `AdvancePrediction` 中限制运行时增长的预测前置量，避免权威包间隙越积越大。
4. 在 `TryReconcileAuthoritativeMove` 和 `ApplyAuthoritativeReplay` 中限制权威回放可重建的预测前置量。
5. 当新的权威回放目标发生超限重定向时，增加显示层目标限速，杜绝 `targetPos` 单包后退或单包急转向造成的瞬移/抖动。
6. `PendingStopActive` 期间，停止对旧移动尾包做本地前推重放，不再把回放目标钉死在本地冻结视图，而是让显示层仅向最新 authority 平滑收敛。
7. 下掉客户端移动热路径默认 trace，避免 `Editor.log` 在实机联调时持续膨胀并反向恶化手感。
8. 将异常诊断 warning 改为默认静默，只保留按需开启的编译期开关。
9. 收紧持续移动/站立状态的 prediction lead 窗口，优先消除平地持续移动时固定的可见 authority gap。
10. 回归停步、撞墙、贴墙滑动、离墙恢复和持续同向平地移动场景，避免恢复“可穿墙”或“松手尾走”。
11. 执行 `dotnet build ET.sln`，记录编译与验证结果。

## 验收标准

- [ ] 持续同向平地移动时，不再出现周期性被拉回
- [ ] 权威包到来时，`targetPos` 不再直接退到当前视图后方引发瞬移/抖动
- [x] 停步等待 `stop ack` 期间，不再出现“view/target 被旧冻结点钉死”的明显卡顿
- [ ] 贴墙滑动仍可沿墙移动，且不穿墙
- [ ] 权威明显偏离时仍能纠正，不引入大瞬移
- [x] 客户端移动热路径默认 trace 已关闭
- [ ] 默认诊断日志关闭后，Unity Editor 下不再因 `Diag*` warning 持续刷屏导致明显卡顿
- [ ] 平地持续移动时，客户端长期 authority gap 明显收敛，不再稳定维持约 `0.3m`
- [x] `dotnet build ET.sln` 通过

## 关联文档

- [客户端移动预测与防穿墙方案](./客户端移动预测与防穿墙方案.md)
- [移动系统问题分析](./移动系统问题分析.md)

## 实现追踪

> 开发完成后由 AI 自动填写

| 步骤 | 完成日期 | 涉及文件 | 偏差说明 |
|------|---------|---------|---------|
| 步骤1 | 2026-03-26 | `Editor.log`、`InputSystemComponentSystem.cs`、`GameObjectComponentSystem.cs` | 已确认根因，待落代码 |
| 步骤2 | 2026-03-26 | `InputSystemComponentSystem.cs`、`GameObjectComponentSystem.cs` | 除活跃输入 lead 保留外，额外修复了同方向 `hold` 发送死分支 |
| 步骤3 | 2026-03-26 | `Test_ClientInputReplayReconciliation_Test.cs` | 补充了持续同向活跃输入 lead 的回归用例 |
| 步骤4 | 2026-03-26 | `Editor.log`、`GameObjectComponentSystem.cs` | 已确认剩余抖动源于 target 回退，并补上显示层回退平滑 |
| 步骤5 | 2026-03-26 | `Editor.log`、`GameObjectComponentSystem.cs` | 已确认更大的抖动来自 target 单包急转向，扩展为“超限重定向限速” |
| 步骤6 | 2026-03-26 | `Test_ClientInputReplayReconciliation_Test.cs` | 已补 target 回退平滑与急转向重定向平滑回归用例 |
| 步骤7 | 2026-03-26 | `dotnet build ET.sln` | 最新一轮修改后编译通过；交互式测试命令受当前终端策略限制未执行 |
| 步骤8 | 2026-03-27 | `Editor.log`、`InputSystemComponentSystem.cs`、`GameObjectComponentSystem.cs` | 已确认剩余抖动源于持续移动阶段预测前置量失控，补上“运行中增长限幅 + 回放重建限幅”双层约束 |
| 步骤9 | 2026-03-27 | `dotnet build ET.sln` | 本轮“双层限幅”接入后重新编译通过，0 warning / 0 error |
| 步骤10 | 2026-03-27 | `Editor.log`、`InputSystemComponentSystem.cs`、`Test_ClientInputReplayReconciliation_Test.cs` | 已确认旧方案把 stop ack 前的回放目标钉死在本地冻结视图，这条决策已被最新日志推翻，需改为“停止前推 + 向权威收敛” |
| 步骤11 | 2026-03-27 | `InputSystemComponentSystem.cs`、`ChangePosition_SyncGameObjectPos.cs`、`M2C_JoystickMoveHandler.cs`、`GameObjectComponentSystem.cs` | 已完成 pending stop 收敛语义修正，并关闭客户端移动热路径默认 trace |
| 步骤12 | 2026-03-27 | `Test_ClientInputReplayReconciliation_Test.cs`、`Test_ClientStopPredictionFreeze_Test.cs`、`dotnet build ET.sln` | 相关编译与自动化回归均通过，等待实机人工验收 |
| 步骤13 | 2026-03-29 | `Editor.log`、`ChangePosition_SyncGameObjectPos.cs`、`InputSystemComponentSystem.cs`、`GameObjectComponentSystem.cs`、`Test_ClientInputReplayReconciliation_Test.cs` | 已确认 Editor 卡顿由 `Diag*` warning 放大，且持续移动长期 authority gap 源于 lead 窗口过大；已改为默认静默诊断并收紧 lead 窗口 |
| 步骤14 | 2026-03-29 | `InputSystemComponentSystem.cs`、`Test_ClientInputReplayReconciliation_Test.cs`、`dotnet build ET.sln`、`dotnet .\Bin\ET.App.dll --Console=1 --SceneName=Test --TestName='Test_Client(InputReplay.*|StopPredictionFreeze_Test)'` | 已将 `PendingStop` 语义和对应 3 条回归断言一起回退到 `94a9c8a7e` 基线，并完成 11/11 自动化回归 |
